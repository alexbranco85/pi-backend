<!DOCTYPE html>
<html lang="PT-BR">

<!-- Include Head -->
<%- include('includes/head', {title: 'Sneakers house' }) %>

    <body>
        <!-- Header -->
        <%- include('includes/header') %>
            <!--/Header -->
            <main class="m-4">
                <section class="container">
                    <div class="login">
                        <h2>LOGIN</h2>
                        <form action="/login" method="POST">
                            <% if (typeof errors !=='undefined' ) { %>
                                <ul>
                                    <% for(let error in errors) { %>
                                        <li>
                                            <%= errors[error].msg %>
                                        </li>
                                        <% } %>
                                </ul>
                                <% } %>
                                    <div id="input-login">
                                        <label for="email" class="form-label">Digite seu E-mail:</label>
                                        <input type="email" id="email" name="email" placeholder="Informe o seu e-mail" class="form-input
                                        <label for="password">Digite sua senha:</label>
                                        <input type="password" id="pwd" name="pwd" placeholder="Informe a senha" class="form-input">
                                    </div>
                                    <div class="button-login">
                                        <button class="botao-vermelho" type="submit">Entrar</button>
                                        <a href="" class="esqueci-senha">Esqueci minha senha</a>
                                    </div>
                        </form>
                    </div>
                    <div class="msg-criarConta">
                        <h3><strong>Ainda não tem uma conta? </strong><a href="http://localhost:3000/cadastro">Clique
                                aqui para criar.</a></h3>
                    </div>
                </section>
            </main>
            <!-- footer -->
            <%- include('includes/footer') %>

                <!--/footer-->

                <div id="modal-login" class="modal-container">
                    <div class="modal">
                        <div class="login">
                            <button class="b-fechar">X</button>
                            <h2>LOGIN</h2>
                            <div id="input-login">
                                <label for="email">Digite seu e-mail</label>
                                <input type="email">
                                <label for="password">Digite sua senha</label>
                                <input type="password">
                            </div>
                            <div class="button-login">
                                <button class="botao-vermelho">Entrar</button>
                                <a href="" class="esqueci-senha">Esqueci minha senha</a>
                            </div>
                            <div class="redesocial-login">
                                <img src="./img/redeSocial/facebook.png" alt="">
                                <span>Entrar com o Facebook</span>
                                <img src="./img/redeSocial/google.png" alt="">
                                <span>Entrar com o Google</span>
                            </div>
                        </div>
                        <div class="msg-criarConta">
                            <h3><strong>Ainda não tem uma conta? </strong><a class="criar-conta">Clique aqui para
                                    criar.</a></h3>
                        </div>
                    </div>
                </div>

    </body>
</html>