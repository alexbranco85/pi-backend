<!DOCTYPE html>
<html>
<!-- Include Head -->
<%- include('includes/head', {title: 'Detalhe do Produto' }) %>

  <body>
    <!-- Header -->
    <%- include('includes/header') %>
    <!-- /Header -->

      <!-- product detail -->
      <div class="container products-wrapper">
        <div class="row">
          <div class="col-12">
            <h2 class="products-title">Detalhe do produto: <%= product.nome %></h2>
          </div>
        </div>
        <div class="product-detail">
          <div class="row">
            <div class="col-12 col-lg-8">
              <img src="/images/products/<%= product.imagemDestaque %>" alt="product image" class="product-detail-img">
              <p class="product-detail-description">
                <%= product.descricao %>
              </p>
            </div>
            <div class="col-12 col-lg-4">
              <article class="product-detail-info">
                <h2 class="product-detail-title">
                  <%= product.nome %>
                </h2>
                <% if(product.desconto> 0) {
                  let finalPrice = (product.valor * ((100 - product.desconto) / 100)).toFixed(0)
                %>
                  <p class="product-detail-price small">
                    <span>R$<%= toThousand(product.valor) %></span>
                    /
                    <b>
                      <%= product.desconto %>% OFF
                    </b>
                  </p>
                  <p class="product-detail-price">
                    R$<%= toThousand(finalPrice) %>
                  </p>
                <% } else { %>
                    <p class="product-detail-price">
                      R$<%= toThousand(product.valor) %>
                    </p>
                <% } %>

                <ul class="actions-list">
                  <li>
                    <i class="fas fa-credit-card"></i>
                    <p>Pague em 12 vezes sem juros</p>
                  </li>
                  <li>
                    <i class="fas fa-store"></i>
                    <p>Retirada gratuita nos locais de venda</p>
                  </li>
                </ul>

                <a href="#" class="buy-now-button">COMPRAR AGORA</a>
                <hr>

                <a href="/product/edit/<%= product.sku %>" class="action-button edit">Editar Produto</a>
                <form action="/product/delete/<%= product.sku %>?_method=DELETE" method="POST" style="display: inline-flex">
                  <button type="submit" class="action-button delete">Remover</button>
                </form>
              </article>
            </div>
          </div>
        </div>
      </div>
      <!-- /product detail -->

      <!-- footer -->
      <%- include('includes/footer') %>
      <!-- /footer -->
  </body>

</html>